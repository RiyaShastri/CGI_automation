# Open incident - Create jobs terminate if documents cannot be loaded (Force-closed or deleted PRCU_USER)

## Execute Query
* Perform database activity for open incident - create jobs terminate if documents cannot be loaded
